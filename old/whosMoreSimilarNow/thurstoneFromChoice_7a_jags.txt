# choice probabilities for perceptual reproduction task
data{
  pi = 3.1415
}
model{
  mu[1] = 0
  mu[2] ~ dunif(0, pi/2)
  for (j in 3:nStimuli){
    mu[j] ~ dunif(0, pi)
  } 
  sigma ~ dunif(0, pi)
  for (t in 1:nTrials){
   # xS[t] ~ dnorm(mu[s[t]], 1/sigma^2)
   # xTmp1[t] = ifelse(xS[t] > pi, xS[t]-pi, xS[t])
   # xTmp2[t] = ifelse(xTmp1[t] < 0, xTmp1[t]+pi, xTmp1[t])
    y[t] ~ dnorm(mu[s[t]], 1/sigma^2)
    #~ dnorm(xTmp2[t], 1/(0.01)^2)
  }
}